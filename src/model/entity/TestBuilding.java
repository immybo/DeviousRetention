package model.entity;

public class TestBuilding extends Building {
    public TestBuilding(double x, double y, double size, int playerNumber, int maxHealth) {
        super(x, y, size, playerNumber, maxHealth);
    }
}
